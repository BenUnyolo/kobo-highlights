/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://www.kobohighlights.com/",
  generateRobotsTxt: true, // (optional)
  // ...other options
};

module.exports = config;
